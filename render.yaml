services:
  - type: web
    name: observatrafego
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app --bind 0.0.0.0:$PORT --timeout 120 --graceful-timeout 30
    autoDeploy: true
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
