services:
  - type: web
    name: acidentes-pe
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "sh -c 'python -m gunicorn server:app --bind 0.0.0.0:${PORT:-8000}'"
    healthCheckPath: /health
